import styles from '../styles/gencolor.module.scss'

function gencolor(props) {
    return (
        <div>
            <h1 className={styles.title}>Generate Colors</h1>
        </div>
    );
}

export default gencolor;